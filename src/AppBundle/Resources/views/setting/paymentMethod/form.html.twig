{% extends "base.html.twig" %}


{%  block body %}

    <h3>Méthode de Paiement</h3>

    {{ dump(form) }}
    {{ form_start(form, {'attr' :{'novalidate':'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}